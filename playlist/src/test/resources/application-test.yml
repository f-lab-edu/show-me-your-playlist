spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/playlist_test?serverTimezone=Asia/Seoul
    username: root
    password: 5679

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update

  config:
    activate:
      on-profile: test

